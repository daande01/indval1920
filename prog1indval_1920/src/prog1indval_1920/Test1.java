package prog1indval_1920;

public class Test1 {

	public static void main(String[] args) {

		int i; // deklarerar variabeln i av datatypen integer

		double d = 12.0; // deklarerat variabeln d samt tilldelar den värdet 12.0

		// deklarering + tilldelning = initsiering

		double d1 = Math.pow(2, 2);

		char c = 'A';

		int chartablenumber = c;

		System.out.println(chartablenumber);

		char cc = (char) chartablenumber;

		System.out.println(cc);

		boolean ljuset = false;

		if (ljuset == false && knapp == 1) {

			pin1.light(high);

			ljuset = true;
		} else if (luset == true && kanpp == 1) {

			pin1.light(LOW);
		}

	}

}
